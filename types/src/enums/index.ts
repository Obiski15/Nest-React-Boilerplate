export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum ClientType {
  WEB = 'web_app',
  MOBILE = 'mobile_app',
}

export enum SubscribableNotificationEventType {
  PROMOTIONAL = 'promotional',
}

export enum NotificationChannel {
  EMAIL = 'email',
  WHATSAPP = 'whatsapp',
  SMS = 'sms',
}
